#include <iostream>
using namespace std;

int main()
{

    // Arithmetic
    // int a = 10, b = 20;
    // cout << (a + b) << endl;
    // cout << (a - b) << endl;
    // cout << (a * b) << endl;
    // cout << (a / b) << endl;
    // cout << (a % b) << endl;

    // Assignment
    // "=" => int num = 10;

    // Relational
    int a = 10, b = 20;
    cout << (a < b) << endl;
    cout << (a > b) << endl;
    cout << (a <= b) << endl;
    cout << (a >= b) << endl;
    cout << (a == b) << endl;
    cout << (a != b) << endl;

    // Logical
    // && (logical and)both cond. must be true
    // int age = 19;
    // int car = 1;
    // if (age >= 18 && car >= 1)
    //     cout << "U can Drive";

    // || (logical or)atleast one cond. must be true
    // int age = 17;
    // int car = 1;
    // if (age >= 18 || car >= 1)
    //     cout << "U can Drive";

    // ! (logical not) change 0 to 1 and if any other number present changes into 0.
    int num = 1;
    cout << (!num) << endl;

    return 0;
}